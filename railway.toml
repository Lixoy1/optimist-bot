# Source redeploy trigger for Legacy Brain v3

[build]
builder = "RAILPACK"

[deploy]
startCommand = "python optimist_entrypoint.py"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ALWAYS"
